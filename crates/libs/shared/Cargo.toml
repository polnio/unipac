[package]
name = "unipac-shared"
version.workspace = true
edition.workspace = true

[features]
plugin-loader = []
headers = ["safer-ffi/headers"]

[dependencies]
libloading = "0.8.5"
safer-ffi.workspace = true
